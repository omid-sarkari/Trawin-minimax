{
  "name": "trawin-minimax",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:behavior-intelligence": "npm run build --workspace=behavior-intelligence",
    "test": "npm run test --workspaces",
    "test:behavior-intelligence": "npm run test --workspace=behavior-intelligence"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}