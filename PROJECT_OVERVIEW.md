# PROJECT_OVERVIEW.md — چشم‌انداز تراوین

> سند چشم‌انداز — «تراوین چیست و به کجا می‌رود»
> مکمل: [ROADMAP_PHASES.md](./ROADMAP_PHASES.md) برای فازبندی، [TECHNICAL_BLUEPRINT.md](./TECHNICAL_BLUEPRINT.md) برای مشخصات فنی سطح پایین (LLD).
> نسخه: ۱.۰ — تاریخ: ۲۰۲۶-۰۸-۲۶

---

## ۱. هویت محصول

**تراوین** یک پلتفرم استخدام، رزومه‌سازی و **مسابقات برنامه‌نویسی تیمی و فردی (در سطح استانی)** است که مهارت واقعی را با شواهد قابل اثبات جایگزین ادعا می‌کند.

تراوین یک سایت آزمون نیست. تراوین یک **لایه اعتماد (Trust Layer)** بین برنامه‌نویس و شرکت است:

| مشکل بازار فعلی | پاسخ تراوین |
|---|---|
| رزومه قابل جعل است | رزومه از داده کانونیکال مشتق می‌شود؛ هیچ چیزی دستی وارد نمی‌شود مگر بخش‌های خوداظهاریِ برچسب‌خورده |
| مدرک تضمین مهارت نیست | هر مهارت تأییدشده با **شواهد عددی** همراه است: چند سؤال، چند آزمون، چند پروژه، چه نمره‌ای |
| پروژه‌ها سطح واقعی را نشان نمی‌دهند | رفتار کدنویسی ثبت و تحلیل می‌شود (پیسـت، زمان اجرا پس از پیسـت، نسبت کد AI) |
| مصاحبه کوتاه خطا می‌کند | مصاحبه روی بستر شواهد قبلی انجام می‌شود (LiveKit) |
| مسابقات گسسته از فرایند استخدام‌اند | نتایج مسابقات تیمی/فردی مستقیماً به‌عنوان شاهد رسمی در رزومه ثبت می‌شود |

## ۲. دو نوع رزومه

### الف) رزومه خصوصی (نسخه دولوپر) — کامل
برای خود کاربر؛ شامل:
- تمام اطلاعات پروفایل و بیوگرافی خوداظهاری (با برچسب صریح «خوداظهاری»)
- همه مهارت‌ها با وضعیت تأیید (none / emerging / verified / expert) و جزئیات شواهد
- تاریخچه کامل آزمون‌ها، تحلیل پیشرفته نقاط قوت/ضعف (Pro)
- آمار کدنویسی و شواهد رفتاری جمع‌بندی‌شده
- بخش‌های سفارشی: پروژه، تجربه کاری، تحصیلات، دستاورد، **مسابقه**
- نشانگر تکمیل رزومه (completeness) با مدل وزنی داده‌محور

### ب) رزومه عمومی (نسخه شرکت) — خلاصه و کنترل‌شده
برای کارفرما/ریکروتر؛ فقط شامل بخش‌هایی که ادمین فعال کرده باشد:
- هویت حرفه‌ای (نام، headline، تکنولوژی اصلی)
- مهارت‌های تأییدشده با نمره
- خلاصه آزمون‌ها و شواهد کدنویسی
- بخش‌های عمومی (پروژه/تجربه/مسابقه)
- ❌ هرگز: تحلیل خصوصی، سیگنال‌های رفتاری داخلی، اطلاعات تماس (مگر فعال شدن توسط ادمین)

**قانون طلایی:** نمای شرکت با CSS مخفی نمی‌شود؛ سرور فیلد غیرمجاز را اصلاً ارسال نمی‌کند (`resume_visibility_rules`).

## ۳. موتور هوش الگوریتمی (نه LLM)

قلب تراوین یک **موتور تصمیم الگوریتمی نسخه‌دار** در دیتابیس است (`engine_versions` → `evaluation_rules` → `rule_versions`). این موتور:
1. خروجی آزمون‌ها (answer_evaluations)، شواهد تجمیعی (تعداد سؤال/آزمون/پروژه) و **تصمیمات جمعی** (نتایج مسابقات تیمی و فردی، آرا و رتبه‌ها) را ورودی می‌گیرد
2. طبق سیاست‌های نسخه‌دار تصمیم می‌گیرد چه چیزی با چه سطحی «تأییدشده» لقب بگیرد
3. خروجی را در **رزومه** ثبت می‌کند (developer_resume_sections برای شواهد مسابقه/پروژه؛ skill_scores/user_skill_states برای مهارت)

هر تغییر سیاست = نسخه جدید؛ هر نتیجه تاریخی به نسخه موتورِ زمان خودش گره خورده و همیشه قابل توضیح است.

**شفافیت AI:** استفاده از هوش مصنوعی مجاز است اما اندازه‌گیری می‌شود. رزومه اعلام می‌کند چه نسبتی از کد AI-assisted بوده — این ضعف نیست، شفافیت است.

## ۴. مسابقات تیمی و فردی (سطح استانی)

ستون سوم محصول که رزومه را از «حاصلجمع آزمون‌ها» به «کارنامه عملی» ارتقا می‌دهد:
- **مسابقات فردی**: کدنویسی انفرادی روی زیرساخت Judge0 با تابلو امتیاز زنده
- **مسابقات تیمی**: تشکیل تیم (کاپیتان + اعضا)، همکاری روی چالش، رتبه‌گیری استانی
- **مقیاس جغرافیایی**: استان به‌عنوان واحد رقابت (province field) — امکان فینال کشوری در آینده
- نتایج تأییدشده مسابقات توسط موتور تصمیم به‌عنوان **شواهد رسمی** وارد رزومه می‌شود (kind=competition) و در تأیید مهارت‌ها وزن دارد

## ۵. ذی‌نفعان

| نقش | ارزش دریافتی | دسترسی |
|---|---|---|
| دولوپر | رزومه زنده معتبر، مسیر رشد شخصی، دیده‌شدن توسط شرکت‌ها، مسابقات استانی | /dashboard |
| شرکت | جستجوی نامزد با مهارت تأییدشده، انتشار شغل، بررسی درخواست‌ها، مصاحبه | /company |
| ادمین | کنترل کامل محتوا (سؤال/آزمون)، تنظیم سیاست تأیید، مدیریت کاربران و رزومه‌ها، مدیریت مسابقات | /admin |

## ۶. مدل درآمد

- **دولوپر Pro**: تحلیل پیشرفته مهارت‌ها، بخش‌های غنی رزومه، بینش‌های پیشرفته (`plans.code='pro'`)
- **شرکت**: جستجوی نامزدهای تأییدشده، انتشار آگهی، خط لوله استخدام (فاز آینده بیلینگ واقعی روی `user_plans` سوار می‌شود)
- **مسابقات**: ظرفیت ثبت‌نام رویدادهای ویژه (آینده)

## ۷. معماری در یک نگاه

```
Next.js 16 (App Router, RTL Dark Premium)
   │
   ├─ API Routes (احراز هویت + گارد نقش) ──► Service Layer ──► Supabase (PostgreSQL)
   │                                                              │
   ├─ Judge0 (اجرای امن کد — فقط coding/debugging/مسابقه)         │
   ├─ LiveKit (مصاحبه تصویری)                                    │
   ├─ Monaco+Yjs (ویرایشگر/همکاری)                                │
   └─ JPlag/OpenProctor (ضدتقلب)                                  ▼
                                              موتور تصمیم الگوریتمی نسخه‌دار
```

اصول ثابت: Database-First · Server-Authoritative · Evidence-Based · Privacy-Aware · Versioned Decisions

## ۸. وضعیت امروز (خلاصه)

✅ احراز هویت نقش‌محور، پنل ادمین کامل، Runtime آزمون کامل با Judge0 fail-closed، آنبردینگ، رزومه زنده دو-نما، Pro/Entitlements، موتور تأیید مهارت نسخه‌دار (۲۰۰ سؤال/۵ آزمون/۳ پروژه)، تست‌های واحد و Live Acceptance

🔜 به ترتیب: پلتفرم شرکت → مسابقات تیمی/فردی استانی → موتور رفتاری v1 → تشابه کد (JPlag) → تطبیقی واقعی → پروفایل عمومی/Snapshot → Scale

جزئیات: [ROADMAP_PHASES.md](./ROADMAP_PHASES.md)
